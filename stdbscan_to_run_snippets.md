# Run everything
python stdbscan_clustering_adj.py all

# Run just training
python stdbscan_clustering_adj.py train

# Run just KNN inference
python stdbscan_clustering_adj.py infer_knn

# Run vizualization part
python stdbscan_clustering_adj.py viz
